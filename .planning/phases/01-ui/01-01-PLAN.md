---
phase: 01-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next/components/source-tabs.tsx
  - next/components/source-tabs.test.tsx
  - next/lib/articles.ts
  - next/lib/articles.test.ts
  - next/app/page.tsx
  - next/e2e/home.spec.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "ユーザーはページ上部に「すべて / Zenn / はてブ / Google ニュース / Togetter」のタブを見ることができる（SRC-01）"
    - "ユーザーがタブをクリックすると、URL を変えずにクライアント内で該当ソースの記事だけに絞り込まれる（SRC-02）"
    - "ソースで絞り込んでも直近3日の記事のみが表示される（既存の表示期間を維持）（SRC-03）"
    - "初回表示時およびリロード後は「すべて」タブが選択された状態で表示される（SRC-04）"
    - "選択したソースに直近3日の記事がない場合、「記事がありません」というメッセージが表示される（SRC-05）"
  artifacts:
    - path: "next/components/source-tabs.tsx"
      provides: "ソース切り替えタブ + クライアント側 sourceId フィルタ（\"use client\"）"
      min_lines: 30
      contains: "use client"
    - path: "next/lib/articles.ts"
      provides: "SOURCE_TABS タブ定義（「すべて」+ 各 SourceId）と filterArticlesBySource ヘルパー"
      contains: "SOURCE_TABS"
    - path: "next/app/page.tsx"
      provides: "RSC が直近3日の全ソース Article[] を取得し SourceTabs に props で渡す"
      contains: "SourceTabs"
  key_links:
    - from: "next/app/page.tsx"
      to: "next/components/source-tabs.tsx"
      via: "views を props で渡す（RSC→Client Component 境界）"
      pattern: "<SourceTabs"
    - from: "next/components/source-tabs.tsx"
      to: "next/lib/articles.ts"
      via: "SOURCE_TABS / filterArticlesBySource を import"
      pattern: "SOURCE_TABS|filterArticlesBySource"
---

<objective>
既存の Next.js フロントエンド（`next/app/page.tsx`）に、ニュースソース別のタブ切り替え UI を追加する。タブは「すべて / Zenn / はてブ / Google ニュース / Togetter」を画面上部に表示し、クリックでクライアント内（URL 非連携）に該当ソースの記事だけへ絞り込む。直近3日フィルタは既存挙動を維持。デフォルトは「すべて」。選択ソースに記事がない場合は「記事がありません」を表示する。

Purpose: ユーザーが見たいソースをすばやく切り替えて記事を閲覧できるようにする（プロジェクトのコアバリュー）。要件 SRC-01〜SRC-05 をすべて満たす。
Output: タブ切り替え用の Client Component（`source-tabs.tsx`）、タブ定義/フィルタヘルパー（`articles.ts` に追加）、`page.tsx` の配線、ユニットテスト、E2E テスト。
</objective>

<phase_goal>

## フェーズゴール

**As a** サイト閲覧者, **I want to** 画面上部のタブでニュースソースを切り替える, **so that** 見たいソースの直近の記事だけをすばやく閲覧できる。

</phase_goal>

<execution_context>
@/Users/tokahana/workspaces/news.hako.tokyo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tokahana/workspaces/news.hako.tokyo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# 既存の実装パターン（変更対象・参照対象）
@next/app/page.tsx
@next/lib/articles.ts
@next/lib/article.ts
@next/components/article-list.tsx
@next/components/empty-state.tsx
@next/components/source-badge.tsx
@next/e2e/home.spec.ts
</context>

<notes>
- **このリポジトリの Next.js は通常と異なる破壊的変更を含む。** コードを書く前に `next/node_modules/next/dist/docs/` 内の該当ガイド（特に Client Component / `"use client"` / RSC からのデータ受け渡しに関する記述）を必ず確認すること（`next/AGENTS.md` の指示）。
- **SourceId の実値は `zenn` / `hatena` / `googlenews` / `togetter`**（`next/lib/article.ts` の `ARTICLE_SOURCES`）。`google-news` ではない点に注意。
- **表示ラベルは既存の `SOURCE_LABEL`（`next/lib/articles.ts`）を再利用する**: `zenn`→"Zenn"、`hatena`→"はてブ"、`googlenews`→"Google ニュース"、`togetter`→"Togetter"。これは既にユーザーが目にしているラベルであり、新たに「はてな」「Google News」を定義し直さない（既存規約の踏襲）。
- import 順序・型 import・named export・kebab-case ファイル名・`data-testid` 命名は CONVENTIONS.md に従う。
- `next/components/` は原則ステートレスだが、本フェーズで唯一クライアント状態（選択中タブ）を持つコンポーネントを追加する。`"use client"` を付与する。
</notes>

<tasks>

<task type="auto">
  <name>Task 1: ソース切り替えの失敗する E2E テストを追加（happy path）</name>
  <files>next/e2e/home.spec.ts</files>
  <read_first>
    - next/e2e/home.spec.ts（既存の `page.getByTestId` / `test.describe` パターン、`article-link` testid の使い方）
    - next/components/source-badge.tsx（`source-badge-${sourceId}` testid 命名から、タブ testid の命名規約を踏襲する）
    - next/lib/article.ts（SourceId 実値 `zenn`/`hatena`/`googlenews`/`togetter`）
  </read_first>
  <action>
    既存の `test.describe("Home page (/)", ...)` ブロックに、ソースタブの happy path を検証する失敗するテストを追加する（MVP 垂直スライスの RED）。検証するふるまい:
    (1) `page.getByTestId("source-tab-all")` が表示され、初回は選択状態を示す `aria-selected="true"` を持つ（SRC-01, SRC-04）。
    (2) ソースタブ群 `source-tab-zenn` / `source-tab-hatena` / `source-tab-googlenews` / `source-tab-togetter` が表示される（SRC-01）。
    (3) いずれかのソースタブ（例 `source-tab-zenn`）をクリックすると `page.url()` が変化しないこと、かつ表示中の各 `article-link` の祖先が当該ソースのバッジ（`source-badge-zenn`）を含むこと、または記事が無ければ `empty-state-message` が表示されること（SRC-02, SRC-03, SRC-05）。
    testid の命名は `source-tab-${sourceId}` および「すべて」は `source-tab-all` とする（既存 `source-badge-${sourceId}` 規約を踏襲）。タブの選択状態は `aria-selected` 属性で表現する想定。このテストは Task 2 実装前は失敗する（タブ要素が存在しないため）。
  </action>
  <verify>
    <automated>cd next && npx playwright test e2e/home.spec.ts 2>&1 | grep -E "source-tab|failed|passed" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `next/e2e/home.spec.ts` に新規 test が追加され、`source-tab-all` / `source-tab-zenn` 等の testid を参照している（`grep -E "source-tab-(all|zenn|hatena|googlenews|togetter)" next/e2e/home.spec.ts` が5件以上ヒット）
    - 追加テストはクリック後に `page.url()` が変化しないことをアサートする（`grep -c "page.url()" next/e2e/home.spec.ts` が1以上）
    - Task 2 実装前の時点でこの新規テストが FAIL すること（RED 確認）。実装完了後（Task 2/3 後）に PASS すること
  </acceptance_criteria>
  <done>ソース切り替えの happy path を検証する E2E テストが追加され、実装前は失敗する（RED 状態）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: タブ定義/フィルタヘルパー追加 + SourceTabs Client Component + page.tsx 配線（最薄スライス）</name>
  <files>next/lib/articles.ts, next/lib/articles.test.ts, next/components/source-tabs.tsx, next/components/source-tabs.test.tsx, next/app/page.tsx</files>
  <read_first>
    - next/lib/articles.ts（`SOURCE_LABEL`、`ArticleListItemView`、`toListItemView`、`filterArticlesWithinDays`、import 順序）
    - next/lib/article.ts（`ARTICLE_SOURCES` 配列とその順序、`SourceId` 型）
    - next/lib/articles.test.ts（Vitest + `InMemoryFileReader` パターン、`describe/it/expect`、import 順序）
    - next/app/page.tsx（RSC のデータ取得・`views` 生成・`EmptyState`/`ArticleList` 分岐・レイアウト構造）
    - next/components/article-list.tsx, next/components/empty-state.tsx（再利用する表示コンポーネントの props 形）
    - next/node_modules/next/dist/docs/（`"use client"` と RSC→Client への props 受け渡しに関する該当ガイド）
  </read_first>
  <behavior>
    - `SOURCE_TABS`: `{ id: "all" | SourceId; label: string }[]`。先頭が `{ id: "all", label: "すべて" }`、続いて `ARTICLE_SOURCES` の順序（zenn, hatena, googlenews, togetter）で各 `{ id, label: SOURCE_LABEL[id] }`。要素数は5。
    - `filterArticlesBySource(views, tabId)`: `tabId === "all"` のとき入力をそのまま返す。それ以外は `view.sourceId === tabId` のものだけを返す。元配列を変異させない。該当なしのとき空配列を返す。
    - `SourceTabs` 初期選択は `"all"`（props で全 `ArticleListItemView[]` を受け取り、初期は全件表示）。
  </behavior>
  <action>
    (A) `next/lib/articles.ts` に以下を追加（named export、UPPER_SNAKE_CASE 定数 / camelCase 関数）:
      - 型 `SourceTabId = "all" | SourceId`
      - 定数 `SOURCE_TABS: ReadonlyArray<{ id: SourceTabId; label: string }>` を上記 behavior の通り `ARTICLE_SOURCES` から導出（「すべて」を先頭に prepend）。`SOURCE_LABEL` を再利用しラベルを引く。
      - 純粋関数 `filterArticlesBySource(views: ReadonlyArray<ArticleListItemView>, tabId: SourceTabId): ArticleListItemView[]`。`tabId === "all"` で全件、それ以外は `sourceId` 一致でフィルタ。`[...input].filter(...)` パターンで非変異。
    (B) `next/lib/articles.test.ts` に Vitest テストを追加: `SOURCE_TABS` の先頭が `all`/"すべて" で長さ5かつ順序が `["all", ...ARTICLE_SOURCES]` であること、`filterArticlesBySource` の all/特定ソース/該当なし(空配列)/非変異の各ケース。
    (C) `next/components/source-tabs.tsx` を新規作成（`"use client"` を先頭行）。`SourceTabsProps { views: ArticleListItemView[] }`。`useState<SourceTabId>("all")` で選択タブを保持。`SOURCE_TABS` を `<nav>` 内の `<button>` 群としてレンダリングし、各ボタンに `data-testid={\`source-tab-${tab.id}\`}`、`aria-selected={selected === tab.id}`、`onClick` で選択更新。`filterArticlesBySource(views, selected)` の結果が空なら既存 `EmptyState` を、それ以外は既存 `ArticleList` を表示。Tailwind v4 クラスで既存トーン（zinc 系、選択時に強調）に合わせる。React import は名前付き（`import { useState } from "react"`）。
    (D) `next/app/page.tsx` を修正: RSC は引き続き直近3日の全ソース `Article[]` を取得・ソートし `views`（`ArticleListItemView[]`）を生成する（SRC-03 維持、データ取得は RSC のまま）。`<main>` 内の `views.length === 0 ? <EmptyState/> : <ArticleList views={views}/>` を `<SourceTabs views={views} />` に置き換える（空/一覧の出し分けは SourceTabs 側に移譲）。`Header`/`Footer` と `stats` は従来通り全件ベースで維持。`EmptyState`/`ArticleList` の直接 import が page で不要になれば削除し、`SourceTabs` を import する。
  </action>
  <verify>
    <automated>cd next && npx vitest run lib/articles.test.ts components/source-tabs.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use client" next/components/source-tabs.tsx` が1（先頭行に `"use client"`）
    - `grep -E "SOURCE_TABS|filterArticlesBySource" next/lib/articles.ts` が2件以上ヒット（定数と関数の定義）
    - `grep "SourceTabs" next/app/page.tsx` がヒットし、かつ `page.tsx` 内で `views` を `SourceTabs` に渡している（`grep "<SourceTabs" next/app/page.tsx` が1以上）
    - `grep -E "source-tab-\$\{|source-tab-" next/components/source-tabs.tsx` がヒット（testid を動的生成）
    - `cd next && npx vitest run lib/articles.test.ts components/source-tabs.test.tsx` が PASS（`SOURCE_TABS` 構造・`filterArticlesBySource` の4ケース）
    - `cd next && npx tsc --noEmit` が型エラーなしで完了
    - `page.tsx` のデータ取得が依然 RSC（`articleRepository.getArticlesPublishedSince` と `filterArticlesWithinDays(..., DISPLAY_WINDOW_DAYS, ...)` の呼び出しが残存。`grep "filterArticlesWithinDays" next/app/page.tsx` が1以上 → SRC-03 維持）
  </acceptance_criteria>
  <done>タブ定義/フィルタヘルパーが追加されユニットテストが通り、`SourceTabs` Client Component が page に配線され、Task 1 の E2E happy path が通る状態になる（SRC-01, SRC-02, SRC-03, SRC-04, SRC-05 の骨格成立）</done>
</task>

<task type="auto">
  <name>Task 3: 空表示・選択状態のスタイル仕上げと E2E グリーン確認</name>
  <files>next/components/source-tabs.tsx, next/e2e/home.spec.ts</files>
  <read_first>
    - next/components/source-tabs.tsx（Task 2 で作成した実装）
    - next/components/empty-state.tsx（「まだ記事がありません」既存文言・`empty-state-message` testid）
    - next/components/header.tsx, next/components/source-badge.tsx（Tailwind トーン・選択強調の参考。zinc 系 + 選択時のアクセント）
    - next/e2e/home.spec.ts（Task 1 で追加したテスト）
  </read_first>
  <action>
    (A) `source-tabs.tsx` の仕上げ: 選択中タブの視覚的強調（例: 選択時に下線/背景アクセント、非選択は `text-zinc-500`）を Tailwind v4 で既存トーンに合わせて調整。`<nav>` に `data-testid="source-tabs"` を付与。タブは横スクロール/折り返しに耐える構造（`flex` + `gap`）にする。アクセシビリティとして各ボタンに `aria-selected` を確実に出力する（SRC-04 のリロード後「すべて」選択は `useState` 初期値で担保済み）。空表示時は選択ソースに記事がない旨が伝わるよう、既存 `EmptyState`（文言「まだ記事がありません」）をそのまま用いる（SRC-05。新規文言を増やさず既存を再利用）。
    (B) Task 1 の E2E テストが PASS することを確認し、必要なら testid・セレクタの整合（Task 2 実装と Task 1 期待の差分）を修正する。`source-tab-all` の初期 `aria-selected="true"`、ソースクリック後の URL 不変、絞り込み or 空表示の各アサーションを確実にグリーンにする。
  </action>
  <verify>
    <automated>cd next && npx playwright test e2e/home.spec.ts 2>&1 | tail -25 && npx eslint components/source-tabs.tsx app/page.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'data-testid="source-tabs"' next/components/source-tabs.tsx` が1件ヒット（nav 全体の testid）
    - `grep -E "aria-selected" next/components/source-tabs.tsx` がヒット（選択状態の表現）
    - `cd next && npx playwright test e2e/home.spec.ts` の全テストが PASS（Task 1 で追加した happy path 含む。RED→GREEN 完了）
    - `cd next && npx eslint components/source-tabs.tsx app/page.tsx` がエラーなし
    - 新規の空表示文言を追加していない（`empty-state.tsx` の既存文言を再利用。`grep -c "まだ記事がありません" next/components/source-tabs.tsx` が0 = SourceTabs 内に重複文言を持たず EmptyState を呼ぶ）
  </acceptance_criteria>
  <done>選択状態のスタイルと空表示が仕上がり、Task 1 で追加した E2E happy path を含む全 E2E テストが PASS、ESLint クリーン。SRC-01〜SRC-05 がエンドツーエンドで成立する</done>
</task>

</tasks>

<threat_model>

## 信頼境界

| Boundary | Description |
|----------|-------------|
| RSC → Client Component | `page.tsx`（サーバー）から `SourceTabs`（クライアント）へ `views` を props で渡す。渡すのはサーバーで既に検証済み（zod パース済み）の `ArticleListItemView[]` のみ。 |

## STRIDE 脅威レジスタ

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | RSC→Client props | accept | 該当する高リスク脅威なし。本サイトは認証なしの公開サイトで、追加するのはクライアント側の表示フィルタのみ（ユーザー入力・サーバー状態変更・新規データ取得・新規ネットワーク呼び出しなし）。`views` は既に公開表示中の記事メタデータであり機微情報を含まない。 |
| T-01-SC | Tampering | npm install | accept | 本プランは新規パッケージを導入しない（React `useState`・既存 Next.js・既存 Vitest/Playwright のみ）。サプライチェーン経路の追加なし。 |

信頼境界の変更なし。クライアント側表示フィルタのみのため、該当する高リスク脅威はない。

</threat_model>

<verification>
フェーズ全体の検証:
- `cd next && npx vitest run` — 既存ユニットテスト + 新規 `articles.test.ts` / `source-tabs.test.tsx` がすべて PASS
- `cd next && npx playwright test e2e/home.spec.ts` — 既存 + 新規ソースタブ E2E がすべて PASS
- `cd next && npx tsc --noEmit` — 型エラーなし
- `cd next && npx eslint` — リントエラーなし
- 手動確認（任意）: `npm run dev` でトップページを開き、「すべて」初期選択 → 各タブクリックで絞り込み → リロードで「すべて」復帰 → 記事のないソースで「まだ記事がありません」表示 → URL が変化しないこと
</verification>

<success_criteria>
- SRC-01: 「すべて / Zenn / はてブ / Google ニュース / Togetter」のタブが画面上部に表示される
- SRC-02: タブクリックで URL 非変更のままクライアント内で該当ソースに絞り込まれる
- SRC-03: 絞り込み後も直近3日の記事のみ表示（RSC のデータ取得・`filterArticlesWithinDays` を維持）
- SRC-04: 初回・リロード後は「すべて」が選択状態（`useState` 初期値 `"all"`）
- SRC-05: 選択ソースに記事がない場合「まだ記事がありません」を表示（既存 `EmptyState` 再利用）
- 全ユニット/E2E テスト・型チェック・ESLint が通る
</success_criteria>

<output>
完了時に `.planning/phases/01-ui/01-01-SUMMARY.md` を作成すること（日本語で記述）
</output>
