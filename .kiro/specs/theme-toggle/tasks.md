# Implementation Plan

## 1. 基盤: スタイル基盤とテーマロジック

- [x] 1.1 (P) Tailwind を class ベースダークモードへ移行する
  - `globals.css` の `@import "tailwindcss";` 直後に class ベースの dark バリアントを宣言し、`prefers-color-scheme` ベースの既定を上書きする
  - 既存の `@media (prefers-color-scheme: dark)` 内の配色変数（背景・前景）を `.dark` セレクタ配下へ移行する
  - 完了条件: `<html>` に `dark` クラスを付与/除去すると、背景・前景色と `dark:` ユーティリティの双方が即座に切り替わる（DevTools で確認可能）
  - _Requirements: 1.2, 1.3, 1.4_
  - _Boundary: globals.css_

- [x] 1.2 (P) テーマの型・定数・純粋ヘルパーを実装する
  - 3つのテーマ値（ライト/ダーク/システム）の型・選択肢一覧・永続化キーを単一箇所に定義する
  - 任意入力を有効なテーマへ正規化する関数（不正値・未保存は既定「システム」へフォールバック）を実装する
  - テーマと OS のダーク選好から実効テーマ（light/dark）を決定する純粋関数を実装する
  - 単体テストとプロパティテストを併置する（任意文字列入力でも正規化結果が必ず有効テーマになることを検証）
  - 完了条件: `npm run test:run` で正規化・実効テーマ解決・全域性のテストがグリーン
  - _Requirements: 1.1, 2.3, 2.4, 4.2_
  - _Boundary: theme.ts_

## 2. コア: テーマ切替 UI

- [x] 2.1 テーマ切替 Client Component を実装する
  - 3つの選択肢をネイティブボタン群として描画し、現在選択を支援技術向けの状態属性で可視化する（キーボード操作可）
  - 選択時に選択値を同一ブラウザ内へ永続化し、実効テーマを `<html>` の `.dark` クラスへ即時反映する（再読み込み不要）
  - マウント時に保存値を正規化して状態を初期化する（既に初期スクリプトが適用済みの DOM に対し冪等に動作）
  - 「システム」選択時のみ OS カラースキーム変更を購読して反映し、ライト/ダーク固定時は購読しない。アンマウント時にリスナーを解放する
  - `localStorage`/`matchMedia` アクセスは例外をガードし、失敗時も切替操作自体は機能させる
  - コンポーネントテストを併置する（3択描画・現在選択の状態表示・選択時の永続化と `.dark` 切替を検証）
  - 完了条件: コンポーネントテストがグリーンで、選択操作により `localStorage` 保存と `.dark` クラス切替が観測できる
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 4.1, 4.2, 5.2, 5.3_
  - _Boundary: theme-toggle.tsx_
  - _Depends: 1.2_

## 3. 統合: 初期化スクリプトとヘッダー配線

- [x] 3.1 (P) FOUC 回避の初期テーマ適用スクリプトを組み込む
  - ルートレイアウトの `<html>` にハイドレーション警告抑制を付与する
  - コンテンツ描画前に同期実行されるインラインスクリプトを `<body>` 直前へ配置し、保存値を読み取り（不正/未保存はシステム扱い、システムは OS 設定で実効判定）`.dark` クラスを初期付与する
  - スクリプトの永続化キーと正規化規則を `theme.ts` の定義と一致させる（import 不可のため値の一致を保つ）
  - 完了条件: 保存テーマがダークの状態でリロードしても、誤配色から正配色への切替（ちらつき）が初期表示で発生しない
  - _Requirements: 2.2, 3.1, 3.2_
  - _Boundary: layout.tsx_
  - _Depends: 1.2_

- [ ] 3.2 (P) ヘッダー右上に切替 UI を配置する
  - 既存ヘッダーの右側にテーマ切替コンポーネントを差し込み、既存の記事件数表示と併置できるようレイアウトを調整する
  - 完了条件: ヘッダー右上にテーマ切替 UI が表示され、操作で配色が切り替わる
  - _Requirements: 5.1_
  - _Boundary: header.tsx_
  - _Depends: 2.1_

## 4. 検証: E2E

- [ ] 4.1 テーマ切替の E2E テストを追加する
  - ダークを選択 → リロード後もダークが維持されることを検証する
  - 初期表示で誤配色から正配色へのちらつきが発生しないことを検証する
  - 完了条件: `npm run test:e2e` で永続化と FOUC 不在の E2E がグリーン
  - _Requirements: 2.1, 2.2, 3.1, 3.2_
  - _Boundary: e2e_
  - _Depends: 3.1, 3.2_

## Implementation Notes
- 2.1: React 19 / eslint-config-next 同梱の `react-hooks/set-state-in-effect` ルールにより、`useEffect` 内 setState（design.md 記載の初期同期パターン）は lint エラーになる。`useSyncExternalStore`（サーバースナップショット=既定 system、クライアント=localStorage 正規化値）で代替。後続タスクで effect 内 setState を避けること。
- 2.1: vitest 環境は `node`（`vitest.config.ts`）で jsdom / @testing-library/react は未インストール。React コンポーネントのクリック相互作用テストは不可。`renderToStaticMarkup` での描画検証＋副作用ヘルパーへのスタブ注入で観測する方式を採用。タスク4.1 の実ブラウザ相互作用は Playwright(E2E) が担当。
